class Solution {
    public int passThePillow(int n, int time) {
        int cycles=time/(n-1);
        int count=time%(n-1);
        if(cycles%2==0){
            return count+1;
        }
        return n-count;
    }
}
